// Creates an array that lists out all of the options for computer generated words.
var wordList = ["cleopatra", "jane austen", "malala", "marie antoinette"];

// Create variables that hold references to the places in the HTML where we want to display things.

// Score variables
var wins = 0;
var guessesRemaining = 10;

// This function is run whenever the user presses a key.
document.onkey = function(event) {
    // For loop
    for (var i = 0;)

}

// determines which key was pressed
var userGuess = event.key;

// If the user presses a correct letter...
if ((userGuess === "c") || (userGuess === "l") || (userGuess === "e") || (userGuess === "o") || (userGuess === "p") ||
    (userGuess === "a") || (userGuess === "t") || (userGuess === "r") || (userGuess === "a")) {
    // then run this code...

}